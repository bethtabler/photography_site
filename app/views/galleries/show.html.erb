<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @gallery.name %>
  <div id="Photos">
  	<% for photos in @gallery.photos %>
  	<%= image_tag painting.image.url(:thumb).to_s %>
  	<div class="name"><%= painting.name %></div>
  	<div class="actions">
  		<%= link_to "edit", edit_photo_path %> |
  		<%= link_to "remove", photo, :confirm => 'Are you sure?',  :method => :delete %>
  	</div>
  </div>
  <% end %>
  <div class ="clear"></div>
</p>

<%= link_to 'Edit', edit_gallery_path(@gallery) %> |
<%= link_to 'Back', galleries_path %>
